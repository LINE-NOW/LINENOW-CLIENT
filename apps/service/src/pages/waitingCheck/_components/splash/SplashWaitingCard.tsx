// import { useLocation } from "react-router-dom";
import * as S from "./Splash.styles";

const SplashWaitingCard = () => {
  // const { state } = useLocation();
  // const { result } = state || {};

  return (
    <S.OverlayWrapper>
      {/* <BoothCardDetail waitingDetail={result.waiting} /> */}
    </S.OverlayWrapper>
  );
};

export default SplashWaitingCard;
